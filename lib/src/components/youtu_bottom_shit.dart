
import 'package:flutter/material.dart';

class youtubottomshit extends StatelessWidget {
  const youtubottomshit({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ClipRRect(
      borderRadius: ,
      child: Container(
        height: 200,
        color: Colors.white,
      ),
    );
  }
}
